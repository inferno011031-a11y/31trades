# dependencies
node_modules/

# environment / secrets — NEVER commit
.env
.env.*
!.env.example

# runtime state & local data (journal data, logs)
data/*
!data/gold/
data/gold/*
!data/gold/feb2024/
!data/gold/jan2024/
!data/gold/2024-01/
!data/gold/2024-02/
server.log
*.log

# local tooling state (keep the preview run doc)
.freebuff/*
!.freebuff/run.md

# TradingView Library (local only - not pushed to git)
charting_library/
datafeeds/
tv-chart-lib/
# NOTE: chart-test.html IS tracked — it is BattleX's own code (HUD, datafeed,
# bridge). The copyright-protected pieces are charting_library/ and datafeeds/.

# Internal tools, upload scripts with secrets, and AI agent artifacts
tools/
scripts/*.tmp
scripts/*.local.*
.agent/
.agents/
.claude/
.codex/
.dsh/
.gemini/
.kiro/
.opencode/
*.md.tmp

